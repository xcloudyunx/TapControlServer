import React from "react";

// import colors from "../config/colors";
// import constants from "../config/constants";

export default function ConnectionArea(props) {
	// const ip = [].concat(...Object.values(require('os').networkInterfaces())).find(x => !x.internal && x.family === 'IPv4')?.address;
	// console.log(ip);
	
	return (
		<div style={styles.container}>
			ConnectionArea
		</div>
	);
}

const styles = {
	container: {
		flex: 5,
	},
};